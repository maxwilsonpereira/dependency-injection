export default function About(props: any) {
  return (
    <div>
      <button className="buttonMax" onClick={props.renderCounter}>
        COUNTER
      </button>
      <h1>ABOUT COMPONENT</h1>
    </div>
  );
}
